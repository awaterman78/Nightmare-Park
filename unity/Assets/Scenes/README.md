# Scenes

Cloud bootstrap will attempt to create this file automatically:

```text
Assets/Scenes/ArenaOne_Test.unity
```

If the automatic build scene creation fails, this folder still tells us the expected scene path.
